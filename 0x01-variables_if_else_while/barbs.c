#include <stdio.h>

/**
 * main - use putchar to print alphabets
 *
 * Return: 0
*/
int main(void)
{
	char x =;

	for (x = 'A' ; x <= 'Z' ; x++);
	{
		putchar(tolower(x));
		putchar('\n');
	}
	return (0);
}

